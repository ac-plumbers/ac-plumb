media:
  - name: service_images
    label: Service Images
    input: src/images/services
    output: /images/services
    path: .
    extensions: [png, jpg, jpeg, webp, svg, avif]
  - name: area_images
    label: Area Images
    input: src/images/areas
    output: /images/areas
    path: .
    extensions: [png, jpg, jpeg, webp, svg, avif]

settings:
  content:
    merge: true

content:
  - name: services
    label: Services
    type: collection
    path: src/content/services
    extension: md

    view:
      primary: title
      fields: [title, sortedOrder, category, featured, tags]
      sort: [title]

    fields:
      - { name: title, label: Title, type: string }
      - { name: slug, label: URL Slug, type: string }
      - { name: description, label: Description, type: text }
      - { name: paragraph, label: Paragraph, type: text }
      - { name: sortedOrder, label: Sort Order, type: number }

      - name: image
        label: Image
        type: image
        options: { media: service_images, path: . }

      - { name: alt, label: Image Alt Text, type: text }
      - { name: titleImage, label: Image Title, type: string }

      - name: imageHero
        label: Hero Image
        type: image
        options: { media: service_images, path: . }

      - { name: altHero, label: Hero Image Alt Text, type: text }
      - { name: titleImageHero, label: Hero Image Title, type: string }

      - name: tags
        label: Tags
        type: string
        list: true

      - { name: featured, label: Featured, type: boolean }
      - { name: category, label: Category, type: string }

      # New fields for richer content
      - { name: intro, label: Intro, type: text }
      - name: benefits
        label: Benefits
        type: string
        list: true
      - name: specialties
        label: Specialties
        type: string
        list: true

      # Features (list of objects)
      - name: features
        label: Features
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{fields.title}"
        fields:
          - { name: title, label: Feature Title, type: string }
          - { name: description, label: Feature Description, type: text }

      # FAQs (list of objects)
      - name: faqs
        label: FAQs
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{fields.question}"
        fields:
          - { name: question, label: Question, type: string }
          - { name: answer, label: Answer, type: text }

      - name: gallery
        label: Gallery
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{fields.alt}"
        fields:
          - name: src
            label: Image
            type: image
            options: { media: service_images, path: . }
          - { name: alt, label: Alt Text, type: text }
          - { name: title, label: Title, type: string }

  - name: areas
    label: Areas
    type: collection
    path: src/content/areas
    extension: md

    view:
      primary: area
      fields: [area, title, sortOrder, level, featured]
      sort: [sortOrder]

    fields:
      - { name: area, label: Area Name, type: string }
      - { name: title, label: Title, type: string }
      - { name: description, label: Description, type: text }

      - name: image
        label: Area Image
        type: image
        options: { media: area_images, path: . }

      - { name: sortOrder, label: Sort Order, type: number }
      - { name: featured, label: Featured, type: boolean }

      - name: services
        label: Services (slugs)
        type: string
        list: true

      - { name: postcode, label: Postcode, type: string }
      - { name: level, label: Coverage Level, type: string }
      - { name: responseTime, label: Response Time, type: string }
      - { name: region, label: Region, type: string }

      - name: neighborhoods
        label: Neighborhoods
        type: string
        list: true

      - name: faqs
        label: FAQs
        type: object
        list:
          collapsible:
            collapsed: true
            summary: "{fields.question}"
        fields:
          - { name: question, label: Question, type: string }
          - { name: answer, label: Answer, type: text }

      - { name: specificContent, label: Specific Content, type: text }

  - name: legal
    label: Legal Pages
    type: collection
    path: src/content/legal
    extension: md

    view:
      primary: page
      fields: [page, title, pubDate]
      sort: [page]

    fields:
      - { name: page, label: Page Name, type: string }
      - { name: pubDate, label: Published Date, type: date }
      - { name: title, label: Title, type: string }
      - { name: description, label: Description, type: text }
      - { name: keywords, label: Keywords, type: text }
      - { name: author, label: Author, type: string }
      - { name: datePublished, label: Date Published (ISO), type: string }
      - { name: dateModified, label: Date Modified (ISO), type: string }
      - name: body
        label: Content
        type: rich-text
