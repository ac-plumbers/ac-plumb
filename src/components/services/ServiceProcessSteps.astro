---
type Step = {
  title: string;
  description: string;
};

type Props = {
  steps: Step[];
};

const { steps } = Astro.props;
/* Content for SEO */
import Text from '../fundations/elements/Text.astro';
---

<section id='service-process' class='py-16 sm:py-24'>
  <div class='mx-auto max-w-7xl px-4 sm:px-6 lg:px-8'>
    <div class='mx-auto max-w-2xl lg:text-center'>
      <Text
        tag='span'
        variant='textBASE'
        class='text-base font-semibold tracking-wide text-primary-600 uppercase'>
        Our Process
      </Text>
      <Text
        tag='h3'
        variant='displayLG'
        class='mt-2 font-bold tracking-tight text-neutral-900'>
        How We Work
      </Text>
      <Text tag='p' variant='textBASE' class='mt-4 text-neutral-700'>
        Our proven process ensures quality results and complete satisfaction
      </Text>
    </div>

    <div class='mx-auto mt-16 max-w-3xl'>
      <ol class='relative border-l border-primary-200'>
        {
          steps.map((step, index) => (
            <li class='mb-10 ml-6'>
              <span class='absolute -left-4 flex h-8 w-8 items-center justify-center rounded-full bg-primary-600 text-white ring-4 ring-white'>
                {index + 1}
              </span>
              <Text
                tag='h4'
                class='mb-1 text-xl font-semibold text-neutral-900'>
                {step.title}
              </Text>
              <Text tag='p' variant='textBASE' class='text-neutral-600'>
                {step.description}
              </Text>
            </li>
          ))
        }
      </ol>
    </div>
  </div>
</section>
