---
import "@/styles/global.css";
import "@fontsource/poppins";
import "@fontsource-variable/inconsolata";

import BaseHead from "@/components/fundations/head/BaseHead.astro";
import Header from "@/components/global/Header.astro";
import HeaderVue from "@/components/global/Header.vue";
import Footer from "@/components/global/Footer.astro";
const { title, description, keywords, author, themeColor } = Astro.props;

// Get current path for active link highlighting
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en-GB" class="h-full scroll-smooth">
  <head>
    <BaseHead
      title={title}
      description={description}
      keywords={keywords}
      author={author}
      themeColor={themeColor}
    />
  </head>
  <body class="h-full">
    <!-- <Header /> -->
    <HeaderVue currentPath={currentPath} client:load />
    <main id="main-content" class="mx-auto flex flex-col">
      <slot />
    </main>
    <Footer />
  </body>
</html>
