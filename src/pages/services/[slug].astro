---
// @ts-nocheck
import ServicesLayout from '@/layouts/ServicesLayout.astro';
import ServiceHeroSection from '@/components/services/ServiceHeroSection.astro';
import ServiceFeaturesList from '@/components/services/ServiceFeaturesList.astro';
import ServiceProcessSteps from '@/components/services/ServiceProcessSteps.astro';
import Testimonials from '@/components/home/Testimonials.astro';
import ServiceFaqList from '@/components/services/ServiceFaqList.astro';
import ServiceCallToAction from '@/components/services/ServiceCallToAction.astro';
import RelatedServicesList from '@/components/services/RelatedServicesList.astro';
import Wrapper from '@/components/fundations/containers/Wrapper.astro';
import Text from '@/components/fundations/elements/Text.astro';
import Link from '@/components/fundations/elements/Link.astro';
import { BUSINESS_CONFIG } from '@/utils/businessConfig';
import type {
  ServiceFeature,
  ServiceStep,
  ServiceFaq,
  ServiceFrontmatter,
} from '@/lib/types';
import { getCollection } from 'astro:content';
import SingleSchemaService from '@/components/fundations/schema-services/SingleSchemaService.astro';
import { Image } from 'astro:assets';
import vaillant from '@/images/brands/vaillant-installer-logo-brighton-boilers.jpg';
import viesmann from '@/images/brands/viessmann-logo-boiler-installation-brighton.jpg';
import bpec from '@/images/brands/bpec-certification-logo-gas-plumbing.png';
import checktrade from '@/images/brands/checkatrade-logo-ac-plumbers-brighton.png';
import gasSafe from '@/images/brands/gas-safe-register-logo-certified-brighton.png';
import oftec from '@/images/brands/oftec-registered-logo-heating-certification-1.png';
import ServiceGallery from '@/components/services/ServiceGallery.astro';
import ServiceBenefits from '@/components/services/ServiceBenefits.astro';
import Badge from '@/components/fundations/elements/Badge.astro';
import { BadgeCheck } from 'lucide-vue-next';

export async function getStaticPaths() {
  const services = await getCollection('services');
  return services.map(page => ({
    params: { slug: page.slug },
    props: { page, allServices: services },
  }));
}

const { page, allServices } = Astro.props as {
  page: {
    data: ServiceFrontmatter;
    slug: string;
    render: () => Promise<{ Content: any }>;
  };
  allServices: Array<{
    data: ServiceFrontmatter;
    slug: string;
  }>;
};
const service = page.data;
const { Content } = await page.render();

const relatedServices = allServices
  .filter(s => s.slug !== page.slug)
  .filter(s => {
    if (!page.data.tags || !s.data.tags) return false;
    return page.data.tags.some(tag => (s.data.tags ?? []).includes(tag));
  })
  .slice(0, 3);

const intro = service.intro || '';
const description = service.description || '';
const paragraph = service.paragraph || '';
const benefits = service.benefits || [];
const specialties = service.specialties || [];
const steps = service.process || [];
const faqs = service.faqs || [];
const features: ServiceFeature[] = service.features || [];
const process: ServiceStep[] = service.process || [];
---

<ServicesLayout frontmatter={service}>
  <SingleSchemaService
    service={page}
    providerName='AC Plumbers Ltd.'
    providerUrl='https://acplumb.co.uk'
  />

  <!-- Hero Section with Service Overview -->
  <ServiceHeroSection
    title={service.title}
    image={service.imageHero ?? ''}
    alt={service.altHero}
    intro={intro}
    slug={page.slug}
  />

  <!-- Main Service Content -->
  <Wrapper
    id='main-content'
    variant='compact'
    class='mx-auto mt-12 px-4 py-16 sm:px-6 sm:py-20 lg:px-8'
    aria-label='Service details'>
    <!-- Main Content -->
    <div class='mb-16 grid items-start gap-12 lg:grid-cols-2'>
      <section aria-labelledby='about-service-heading'>
        <article>
          <header>
            <Badge
              text='badge temporaneo soprattutto i colori'
              iconSize='lg'
              class='my-4'>
              <BadgeCheck slot='left-icon' aria-hidden='true' />
            </Badge>
            <Text
              tag='h2'
              variant='displayMD'
              id='about-service-heading'
              class='mb-4 font-bold text-primary-900'>
              {service.title}
            </Text>
          </header>

          <div class='space-y-6 text-neutral-700'>
            <section aria-labelledby='service-description-heading'>
              <!-- content -->
              <Content />
            </section>
          </div>
        </article>
      </section>

      <!-- Sidebar -->
      <aside
        class='rounded-2xl border border-slate-200 bg-white p-8 shadow-xl lg:sticky lg:top-8'
        aria-label='Service benefits and emergency contact information'
        data-bg='dark'>
        <ServiceBenefits benefits={benefits} />
        <div class='mt-4 border-t border-primary-700 pt-4'>
          <Text
            tag='h4'
            variant='displayBase'
            class='mb-2 font-lato font-semibold'>Emergency Service</Text
          >
          <Text tag='p' variant='textBASE' class='mb-2 text-sm text-primary-200'
            >24/7 availability</Text
          >
          <a
            href='tel:00447535653850'
            class='font-semibold text-accent-400 hover:text-accent-300'
            aria-label='Call emergency plumbing service'>
            {BUSINESS_CONFIG.mobilePhone}
          </a>
        </div>
      </aside>
    </div>
    <!-- ✨ NEW: Service Image Gallery -->
    <ServiceGallery
      images={page.data.gallery}
      title={page.data.title}
      paragraph={service.paragraph || ''}
    />

    <!-- Key Features/Benefits -->
    <section aria-labelledby='features-heading'>
      <ServiceFeaturesList features={features} />
    </section>

    <!-- Service Process/Steps -->
    <!-- <section aria-labelledby='process-heading'>
    <ServiceProcessSteps steps={steps} />
  </section> -->

    <!-- Brand / Equipment Section -->
    <!--  <section aria-labelledby='brands-heading' class='bg-primary-900 py-16'>
    <div class='mx-auto grid max-w-7xl gap-12 px-6 lg:grid-cols-2 lg:items-center lg:px-8'>
      <div>
        <Text
          tag='h2'
          variant='displayXL'
          id='brands-heading'
          class='font-bold text-white'>
          Brands & Equipment We Trust
        </Text>
        <Text tag='p' variant='textBase' class='mt-4 text-primary-100'>
          We work with leading manufacturers and use professional-grade tools
          and parts to keep your system reliable, efficient, and safe.
        </Text>
        <ul class='mt-6 space-y-2 text-sm text-primary-100'>
          <li class='flex items-center'>
            <svg
              class='mr-2 h-4 w-4 text-accent-400'
              fill='currentColor'
              viewBox='0 0 20 20'
              aria-hidden='true'
              focusable='false'>
              <path
                fill-rule='evenodd'
                d='M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z'
                clip-rule='evenodd'
              />
            </svg>
            Boilers, cylinders, and central heating components
          </li>
          <li class='flex items-center'>
            <svg
              class='mr-2 h-4 w-4 text-accent-400'
              fill='currentColor'
              viewBox='0 0 20 20'
              aria-hidden='true'
              focusable='false'>
              <path
                fill-rule='evenodd'
                d='M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z'
                clip-rule='evenodd'
              />
            </svg>
            Radiators, valves, and pipework upgrades
          </li>
          <li class='flex items-center'>
            <svg
              class='mr-2 h-4 w-4 text-accent-400'
              fill='currentColor'
              viewBox='0 0 20 20'
              aria-hidden='true'
              focusable='false'>
              <path
                fill-rule='evenodd'
                d='M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z'
                clip-rule='evenodd'
              />
            </svg>
            Smart controls and efficiency improvements
          </li>
          <li class='flex items-center'>
            <svg
              class='mr-2 h-4 w-4 text-accent-400'
              fill='currentColor'
              viewBox='0 0 20 20'
              aria-hidden='true'
              focusable='false'>
              <path
                fill-rule='evenodd'
                d='M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z'
                clip-rule='evenodd'
              />
            </svg>
            Gas safety testing and compliant fittings
          </li>
        </ul>
      </div>
      <div
        class='grid grid-cols-2 items-center gap-6 rounded-2xl bg-primary-800/60 p-6 sm:grid-cols-3'
        aria-label='Brands and certifications'>
        <Image
          width={158}
          height={48}
          src={vaillant}
          alt='Vaillant Advance Installer certification'
          class='max-h-12 w-full object-contain brightness-0 invert'
          format='webp'
          quality={90}
        />
        <Image
          width={158}
          height={48}
          src={viesmann}
          alt='Viessmann certified installer'
          class='max-h-12 w-full object-contain brightness-0 invert'
          format='webp'
          quality={90}
        />
        <Image
          width={158}
          height={48}
          src={bpec}
          alt='BPEC certified gas and plumbing professional'
          class='max-h-12 w-full object-contain brightness-0 invert'
          format='webp'
          quality={90}
        />
        <Image
          width={158}
          height={48}
          src={checktrade}
          alt='Checkatrade verified tradesperson'
          class='max-h-12 w-full object-contain brightness-0 invert'
          format='webp'
          quality={90}
        />
        <Image
          width={158}
          height={48}
          src={gasSafe}
          alt='Gas Safe Register certified engineer'
          class='max-h-12 w-full object-contain brightness-0 invert'
          format='webp'
          quality={90}
        />
        <Image
          width={158}
          height={48}
          src={oftec}
          alt='OFTEC registered heating certification'
          class='max-h-12 w-full object-contain brightness-0 invert'
          format='webp'
          quality={90}
        />
      </div>
    </div>
  </section> -->

    <!-- Service-Specific Testimonials -->
    <section aria-labelledby='testimonials-heading'>
      <Testimonials />
    </section>

    <!-- Service-Specific FAQs -->
    <section aria-labelledby='faqs-heading'>
      <ServiceFaqList questions={faqs} />
    </section>

    <!-- Related Services -->
    <nav aria-label='Related services'>
      <RelatedServicesList services={relatedServices} />
    </nav>

    <!-- Call to Action -->
    <section aria-labelledby='cta-heading'>
      <ServiceCallToAction
        title={`Ready to get started with ${page.data.title}?`}
        buttonText='Book a Service'
        buttonLink='/contact-us'
      />
    </section>
  </Wrapper>
</ServicesLayout>
